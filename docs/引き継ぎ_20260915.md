# 引き継ぎメモ（2026-09-15・健康経営のセッションで作業した分）

このメモは、別プロジェクト（kenkousupport）のセッションでパンしりとりを触ってしまった分の記録。
次回はこのリポジトリ（C:\Users\user\Documents\Claude\Projects\shiritori）を開いたセッションで続ける。

## 今日入れた変更（すべて main に push 済み・GitHub Pages に配備済み）
1. スマホ表示の修正：プロフィール入力（日付欄・文字サイズ）、遊ぶ画面の解答欄（行ごとに明示的に組む。5枚は1行、6枚は3+3、7枚は4+3）、クリア画面（語カードの折り返し・縦スクロール）、ホームの「スコアアタック」ボタンの折り返し
2. 上部の「◯まい」バッジを非表示（スコアアタックの「3 / 5もんめ」だけ表示）。バッジは折り返さず、入らない時は次の行へ
3. カテゴリ名を漢字に（しょう1→小1、ちゅうがく→中学）`src/types.ts` CATEGORY_LABEL
4. スコアアタック：ヒント1回ごとに −5点（`src/ranking.ts` HINT_PENALTY、`hintCount` を play.ts で数える）。ヒントなしは +10点のまま
5. 検索対策：title・description・OGP・JSON-LD・画面下の説明文（index.html の #about）・`public/robots.txt`・`public/sitemap.xml`
6. ホーム画面に追加（ブックマーク）の案内：`src/ui/install.ts`。Chrome は beforeinstallprompt でインストール確認、iPhone/PC は手順表示。QR画面に同梱＋ホーム下に「📲 ホームに 追加」

## 有澤さん側でやること
- Google Search Console（ak-base.com ドメインプロパティ）に `https://shiritori.ak-base.com/sitemap.xml` を送信、トップURLの「インデックス登録をリクエスト」
- 会社HP（mare-website）の自社事例カード PR #31 をマージ → https://github.com/kenichi-sketch/mare-website/pull/31

## 確認のしかた
- `npm run build` → `python -m http.server 4173 --directory dist` → ブラウザをスマホ幅（375px）で。`?demo=home|play|clear` で各画面。レベル4/5は localStorage の profiles に `unlockAll: true` を入れると開く
